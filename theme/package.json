{
  "name": "project-name",
  "description": "project-description",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "concurrently --raw 'roll dev' 'wp-now start --blueprint=blueprint.dev.json'",
    "dev": "roll dev",
    "build": "roll build",
    "format": "roll format",
    "postinstall": "roll install",
    "update": "roll update"
  },
  "dependencies": {},
  "devDependencies": {
    "@tangible/now": "1",
    "@tangible/php-beautify": "^1.0.1",
    "@tangible/roller": "2",
    "concurrently": "^9.0.1"
  }
}
